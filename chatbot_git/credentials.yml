# Eatsplorer - RASA Channel Credentials

# REST channel (for web integration / API testing)
rest:

# Socketio channel (for the web-based chat interface)
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true
