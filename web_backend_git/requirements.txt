# ─────────────────────────────────────────────────────────────
# Eatsplorer Web Backend — Python 3.10 or 3.11+ both fine
# Install in its own venv: python3 -m venv venv_web
# SEPARATE from the chatbot venv — pydantic v2 vs v1 conflict
# ─────────────────────────────────────────────────────────────

fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
pydantic==2.7.1
pandas==2.0.3
python-multipart==0.0.9
